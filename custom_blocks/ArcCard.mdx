---
name: ArcCard
---
---
name: ArcCard
---
<div style={{
  background: 'linear-gradient(135deg, #1a1a2e 0%, #16213e 100%)',
  border: '2px solid #f5c842',
  borderRadius: '12px',
  padding: '24px',
  marginBottom: '24px',
  boxShadow: '0 4px 20px rgba(245, 200, 66, 0.15)'
}}>
  <div style={{
    display: 'flex',
    alignItems: 'center',
    gap: '12px',
    marginBottom: '20px',
    borderBottom: '1px solid #f5c842',
    paddingBottom: '12px'
  }}>
    <span style={{fontSize: '28px'}}>🏴‍☠️</span>
    <h2 style={{
      color: '#f5c842',
      margin: '0',
      fontSize: '22px',
      fontWeight: 'bold'
    }}>{props.name}</h2>
  </div>
  <div style={{
    display: 'grid',
    gridTemplateColumns: '1fr 1fr',
    gap: '16px'
  }}>
    <div style={{
      backgroundColor: 'rgba(245, 200, 66, 0.08)',
      borderRadius: '8px',
      padding: '12px'
    }}>
      <div style={{
        color: '#f5c842',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>📺 EPISODES</div>
      <div style={{color: '#ffffff'}}>{props.episodes}</div>
    </div>
    <div style={{
      backgroundColor: 'rgba(245, 200, 66, 0.08)',
      borderRadius: '8px',
      padding: '12px'
    }}>
      <div style={{
        color: '#f5c842',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>⏭️ SKIP</div>
      <div style={{color: '#ffffff'}}>{props.skip}</div>
    </div>
    <div style={{
      backgroundColor: 'rgba(245, 200, 66, 0.08)',
      borderRadius: '8px',
      padding: '12px'
    }}>
      <div style={{
        color: '#f5c842',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>🎭 VIBE</div>
      <div style={{color: '#ffffff'}}>{props.vibe}</div>
    </div>
    <div style={{
      backgroundColor: 'rgba(245, 200, 66, 0.08)',
      borderRadius: '8px',
      padding: '12px'
    }}>
      <div style={{
        color: '#f5c842',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>🎵 MUSIC</div>
      <div style={{color: '#ffffff'}}>{props.music}</div>
    </div>
    <div style={{
      backgroundColor: 'rgba(245, 200, 66, 0.08)',
      borderRadius: '8px',
      padding: '12px'
    }}>
      <div style={{
        color: '#f5c842',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>🎨 ANIMATION ERA</div>
      <div style={{color: '#ffffff'}}>{props.animation}</div>
    </div>
    <div style={{
      backgroundColor: 'rgba(192, 57, 43, 0.15)',
      borderRadius: '8px',
      padding: '12px'
    }}>
      <div style={{
        color: '#C0392B',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>⚡ DIFFICULTY</div>
      <div style={{color: '#ffffff'}}>{props.difficulty}</div>
    </div>
    <div style={{
      padding: '12px',
      backgroundColor: 'rgba(245, 200, 66, 0.05)',
      borderRadius: '8px',
      borderLeft: '3px solid #f5c842',
      gridColumn: 'span 2'
    }}>
      <div style={{
        color: '#f5c842',
        fontSize: '12px',
        fontWeight: 'bold',
        letterSpacing: '1px',
        marginBottom: '6px'
      }}>🗺️ ONE LINE TEASE</div>
      <div style={{
        color: '#ffffff',
        fontStyle: 'italic'
      }}>{props.tease}</div>
    </div>
  </div>
</div>